proc shared(int x) gives int {
  give x + 1;
}

proc hot_loop() gives int {
  auto total = 0;
  for int i in range(1, 5001) {
    total = total + shared(i);
  }
  give total;
}

proc cold_once() gives int {
  give shared(1);
}

proc main() gives int {
  give hot_loop() + cold_once();
}
